# Drop features from a `geo_json` FeatureCollection with null geometries

Drop features from a `geo_json` FeatureCollection with null geometries

## Usage

``` r
drop_null_geometries(x)
```

## Arguments

- x:

  a `geo_json` FeatureCollection

## Value

a `geo_json` FeatureCollection with Features with null geometries
removed
